    // int c = topcheck(ary, n, m);
    // int d = downcheck(ary, n, m);
    // int e = leftup_diagcheck(ary, n, m);
    // int f = rightup_diagcheck(ary, n, m);
    // int g = leftdown_diagcheck(ary, n, m);
    // int h = rightdown_diagcheck(ary, n, m);

    // if(a==b==c==d==e==f==g==h==1){return 1;}
